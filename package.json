{
  "name": "my-project",
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "homepage": "https://fgtucv.github.io/chart-js/",
  "scripts": {
    "start": "parcel --no-cache",
    "build": "parcel build src/*.html --public-url /chart-js/",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "chart.js": "^4.4.3",
    "gh-pages": "^6.1.1",
    "parcel": "^2.12.0"
  }
}
